import { SegmentGroup } from '@chakra-ui/react';
import {Root, SegmentGroupItems} from './styled';
import PageTemplate from '../../components/templates/PageTemplate';

const Main: React.FC = () => {

  return (
    <Root>
      <PageTemplate>
        {null}
      </PageTemplate>
    </Root>
  ) 
}

export default Main;